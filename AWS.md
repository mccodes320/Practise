#### AWS Artifact

用於按需訪問 AWS 的安全與合規報告，以及某些線上協議。它旨在幫助客戶了解 AWS 雲的安全態勢和合規性，
並驗證他們的雲環境是否符合各種法規、行業標準和內部審計要求。

#### AWS Audit Manager

持續審計您的 AWS 使用情況，以便輕鬆評估您的雲端運營是否符合監管和行業標準。

 AWS Artifact 是提供 AWS 本身的安全合規報告（AWS 作為雲提供商的合規性證明），
 AWS Audit Manager 則是幫助您作為客戶，自動收集和管理證據，以證明您在雲中運行的應用程式和工作負載符合特定的合規標準。


### Partial dependence plots

Partial Dependence Plots（部分依賴圖，簡稱 PDP）是用來解釋機器學習模型中 特定特徵對預測結果的影響 的視覺化工具，常見於解釋 非線性模型

觀察每一個自變數的變化是如何影響預測表現，它可以快速地分析自變數與目標變數之間的關係

### 機器學習模型的準確率（Accuracy in AWS ML services）

訓練模型的「準確率（Accuracy）」，那它是衡量分類模型預測正確率的指標，計算公式如下：

<img width="303" height="61" alt="image" src="https://github.com/user-attachments/assets/0020a1d9-3ccf-4d0a-a4e7-e678102286d4" />

AWS SageMaker 在訓練模型後可以自動產出如：

Accuracy

Precision / Recall

F1 Score

Confusion Matrix（混淆矩陣）





### 
Transparency 透明度
Explainability 可解釋性

Model convergence tables 模型收斂表
用於評估模型訓練過程是否穩定，以及模型是否達到最佳狀態。

Partial Dependence Plots (PDPs) 
提供了一種視覺化的方式來展示一個或兩個特徵如何影響模型的預測。它們可以幫助利害關係人理解：
- 哪些特徵對預測的影響最大。
- 這些特徵的變化如何導致預測的變化。
- 模型的行為是否符合直覺或業務邏輯。


Decision trees 決策樹
- 分類能力：決策樹是一種強大的分類演算法，非常適合將資料分類到多個類別（如 20 種類別）。
- 可解釋性：決策樹以其高可解釋性而聞名。你可以將決策樹的結構直接視覺化出來，它顯示了一系列「如果...那麼...」的規則，從根節點到葉節點的路徑就是模型做出特定分類決策的邏輯。這種基於規則的結構使得其內部機制如何影響輸出變得非常清晰和容易理解，即使是非技術人員也能相對容易地掌握。這完全符合「文件化模型內部機制如何影響輸出」的要求。

Linear regression (線性迴歸)：
- 分類能力：線性迴歸是用於預測連續數值的迴歸演算法，而非分類演算法。它無法直接將基因分為 20 種類別。

Logistic regression (邏輯迴歸)：
- 分類能力：邏輯迴歸是一種分類演算法，可以用於二元分類，也可以擴展到多元分類（如 20 種類別）。
- 可解釋性：邏輯迴歸也具有一定的可解釋性，透過檢視每個特徵的係數，可以了解該特徵對預測類別機率的影響方向和強度。然而，相較於決策樹提供的直接、層次分明的決策路徑，邏輯迴歸的解釋更多是關於特徵權重的統計意義，對於完全理解「內部機制如何導致特定輸出」可能不如決策樹那樣直觀和直接。

Neural networks (神經網路)：
- 分類能力：神經網路，特別是深度學習模型，在複雜的分類任務上表現卓越，可以輕鬆處理 20 種類別的分類。
- 可解釋性：然而，神經網路通常被視為「黑箱模型」。它們的內部結構（數百萬甚至數十億個權重和偏差）非常複雜，很難直接解釋或文件化其內部機制如何影響每個特定的輸出。雖然存在一些事後解釋性技術 (e.g., SHAP, LIME)，但它們無法提供像決策樹那樣清晰、內建的決策路徑解釋。

Amazon SageMaker

進行機器學習推論的需求，其特點是：
- 生產環境 (Production environment)
- 大型輸入數據 (Input data sizes up to 1 GB)
- 長時間處理 (Processing times up to 1 hour)
- 近乎即時的延遲 (Near real-time latency)

Use transfer learning 遷移學習
取一個已經在大型數據集（通常是通用領域）上訓練好的模型（預訓練模型），然後將其用於一個新的、但相關的任務。通常會透過以下方式實現：
- 將預訓練模型的學習到的特徵提取器部分保留下來。
- 移除模型的輸出層，並添加一個新的輸出層，針對新任務的類別或目標進行訓練。
- 選擇性地對預訓練模型的部分或全部層進行微調 (fine-tuning)，使其更好地適應新任務的數據。
- 這種方法可以顯著減少訓練時間和所需的數據量，因為模型已經從大量的通用數據中學習了許多有用的特徵。這完全符合「避免從頭開始創建新模型」和「適應預訓練模型以用於新的、相關任務」的要求。

Human-in-the-loop, HITL
人機協同驗證。對於需要高準確度並最大程度減少錯誤的任務，人工審核和驗證是至關重要的品質控制環節

Amazon SageMaker Ground Truth Plus：
這是一個全託管服務，提供專業的人工團隊來執行數據標註和驗證工作，專為獲取高品質的標註數據而設計。

Data augmentation
這是一種擴充訓練數據集的技術，例如旋轉圖像、增加雜訊等，旨在提高模型泛化能力。它本身不是一種生成新圖像的工具，也無法直接「最小化錯誤標註」。
 
Amazon Bedrock knowledge base：這是為大型語言模型 (LLM) 提供檢索增強生成 (RAG) 功能的，主要用於文字，與圖像生成和標註無關。


Amazon QuickSight Q 進行數據彙總
 是一個商業智慧 (BI) 服務，允許用戶透過自然語言查詢數據並獲得可視化的彙總。這與圖像生成或錯誤標註的風險完全無關

a foundation model (FM) 

Amazon Bedrock
Amazon S3 bucket

Amazon S3 managed keys (SSE-S3)

small language models (SLMs) on edge devices

 large language models (LLMs) on edge devices


## AWS 服務
1. Amazon Rekognition:圖像和影片分析服務

2. Amazon SageMaker Feature Store 特徵商店
- 特徵商店 (Feature Store) 是一個集中式的儲存庫，用於儲存、管理和共享機器學習模型的特徵 (features)。
- 它允許不同的團隊輕鬆地發現、存取和重複使用已經建立好的特徵，而無需重複開發。
- 它提供版本控制、元數據管理、線上/離線存取等功能，方便多個團隊協作和管理特徵。
- 完全符合需求：特徵商店是專為解決跨團隊共享和管理機器學習特徵而設計的。


3. Amazon SageMaker Data Wrangler
 - 準備和轉換數據的工具，例如數據清理、特徵工程、數據可視化等。它主要用於數據預處理階段，而不是用於共享和管理已經建立好的特徵。

### Amazon SageMaker Clarify ###
 - 檢測和減輕機器學習模型的偏差 (bias)，並提供模型可解釋性 (explainability) 的洞察。
 - Identifies potential bias during data preparation 在資料準備階段識別潛在的偏見

### Amazon SageMaker Model Cards ###
 - 用於記錄機器學習模型元數據（例如模型目的、訓練數據、性能指標）的標準化格式。雖然它有助於模型的可追溯性和透明度，但它不是用於共享和管理模型開發中使用的變數（特徵）。

### Amazon Polly ###
文字轉語音服務

### Amazon Rekognition ###
圖像和影片分析服務
 
Amazon SageMaker


Use zero-shot prompts
Use negative prompts
Use positive prompts
Use ambiguous prompts
使用零樣本提示。
使用負面提示。
使用正面提示。
使用模棱兩可的提示。

A. Use zero-shot prompts (使用零次提示)：零次提示是指在沒有任何範例的情況下，直接向模型提出請求。這是一種提示技術的模式，但它本身並不能解決生成無關圖像的問題。事實上，如果提示不夠精確，零次提示可能會導致更多隨機或無關的結果。

Use negative prompts (使用負面提示)：
負面提示 (Negative prompting) 是一種非常有效的技術，用於告訴生成模型「不要」包含哪些元素或屬性。如果模型生成與提示無關的圖像，通常是因為它引入了一些不希望的特徵、風格或雜訊。透過在負面提示中明確指定這些不希望的元素，可以有效地引導模型避免生成它們，從而使最終圖像更接近主要（正面）提示的意圖，減少無關的內容。例如，如果生成的人物圖像總是有畸形的手，可以將「deformed hands」加入負面提示。


Use positive prompts (使用正面提示)：
正面提示 (Positive prompting) 是指你希望模型在圖像中包含的內容。雖然寫一個好的、詳細的正面提示非常重要，但僅僅使用正面提示本身可能不足以解決生成無關圖像的問題，特別是當模型有引入隨機或不相關元素的傾向時。

Use ambiguous prompts (使用模糊提示)：
使用模糊提示會導致模型生成更多隨機、不可預測或與用戶意圖相去甚遠的圖像，這與減少無關圖像的目標完全相
 
擬合 (Overfitting) 問題
是指模型在訓練資料集上表現得非常好，但在未見過的新資料上表現不佳。這意味著模型學到了訓練資料中的雜訊或特定模式，而不是資料底層的普遍關係。



Amazon Aurora PostgreSQL：

Amazon Aurora 是一種與 MySQL 和 PostgreSQL 相容的關聯式資料庫服務。重要的是，Amazon Aurora PostgreSQL 支援 pgvector 擴充功能。pgvector 是一個 PostgreSQL 擴充功能，允許您將向量儲存在 PostgreSQL 資料庫中，並執行向量相似性搜尋（例如，最近鄰搜尋）。這使得 Aurora PostgreSQL 成為儲存和查詢來自生成式 AI 模型的嵌入的絕佳選擇。許多智慧代理和 RAG 應用程式會將其知識庫轉換為向量嵌入，並將其儲存在支援向量搜尋的資料庫中。


Amazon Athena：
Amazon Athena 是一種互動式查詢服務，允許您使用標準 SQL 直接分析 Amazon S3 中的資料。它主要用於無伺服器的大數據分析，不提供內建的向量儲存和高效的向量相似性查詢功能。

Amazon Redshift：
Amazon Redshift 是一種快速、完全受管的資料倉儲服務。它針對分析查詢和大規模資料儲存進行了優化。雖然 Redshift 也可以處理大量數據，但它不是專為高效的向量儲存和相似性搜尋而設計的。

Amazon EMR：
Amazon EMR (Elastic MapReduce) 是一種雲端大數據平台，用於執行 Apache Spark、Hive、HBase、Flink 等開源框架。它主要用於大數據處理和分析，而不是作為一個具備高效向量儲存和查詢能力的資料庫服務。

Model complexity
模型的複雜度與其可解釋性通常呈反比關係。

- 低複雜度模型（如線性迴歸、決策樹）通常被認為是「白盒」模型，它們的決策過程相對容易理解和解釋。您可以清楚地看到哪些特徵如何影響最終預測。
- 高複雜度模型（如深度神經網路、許多基礎模型 FM）通常被稱為「黑盒」模型，因為它們內部運作的複雜性使得追蹤和理解其決策路徑變得非常困難。當模型有數十億甚至數萬億的參數時，很難具體解釋為什麼它會對某個特定的輸入產生某個特定的輸出。
- 在金融機構貸款審批這種高風險且需要高度信任和合規的應用中，可解釋性至關重要，因此模型複雜度是影響可解釋性的關鍵因素。


Logistic regression model (邏輯迴歸模型)：邏輯迴歸模型的預測基於輸入特徵的組合。每個特徵都有一個明確的權重（或係數），表示該特徵對預測結果的影響程度和方向。
- 這些權重是可直接查看和理解的。員工可以清楚地看到「年齡」或「收入」等變數對「潛在客戶」分數的影響有多大。
- 最重要的是，邏輯迴歸模型允許相對容易地手動調整這些權重。基於領域知識，員工可以直接增加或減少某些特徵的權重，以反映他們認為這些特徵在特定情境下應有的重要性。這完美符合了「查看和調整權重」的需求。


- B. Deep learning model built on principal components：深度學習模型（例如多層感知器、卷積神經網路等）通常是非常複雜的「黑箱」模型。它們學習到的特徵表示和權重分佈在多個隱藏層中，難以直接解釋每個原始輸入變數對最終預測的具體貢獻。而「基於主成分 (Principal Components)」雖然有助於降維，但深度學習的內在複雜性使得手動調整單個原始變數的權重幾乎不可能。
- C. K-nearest neighbors (k-NN) model：K-NN 是一種基於實例的學習演算法。它不學習顯式的模型權重。它的預測是基於與新數據點最相似的 K 個訓練數據點的類別。雖然可以為特徵加權以定義「距離」，但這種權重通常是在模型訓練或配置時設定的，而不是在模型應用後讓終端員工直接「查看和調整」每個變數對個別預測的權重。
- D. Neural network：類神經網路是深度學習模型的一種。它們的複雜性很高，由許多層和數百萬甚至數十億個參數組成。儘管每個連接都有一個權重，但這些權重在深層網路中是高度抽象和相互依賴的，使得單獨調整或解釋某個原始輸入特徵的權重變得極其困難和不切實際。


A. Supervised learning (監督式學習)：監督式學習的定義就是從帶有標籤的數據中學習輸入（圖像）到輸出（動物類型分類）的映射。在圖像分類任務中，每個圖像都有一個對應的類別標籤（例如，「貓」、「狗」、「鳥」）。模型的目標是學習如何根據輸入圖像預測其正確的類別標籤。由於公司已經擁有大量帶有標籤的圖像資料集，監督式學習是訓練分類模型的最佳選擇。
- B. Unsupervised learning (非監督式學習)：非監督式學習處理沒有標籤的數據。它的目標是發現數據中的隱藏模式或結構，例如聚類（將相似數據分組）或降維。雖然非監督式學習在某些情況下可以作為預處理步驟，但它本身無法直接完成「分類」任務，因為它沒有類別標籤作為學習目標。
- C. Reinforcement learning (強化學習)：強化學習涉及代理人透過與環境互動來學習如何最大化獎勵的決策過程。它通常用於訓練玩遊戲、機器人控制或自動駕駛等任務。它與圖像分類的靜態預測任務不符。
- D. Active learning (主動學習)：主動學習是一種機器學習方法，模型可以主動選擇它想要標籤的新數據，以提高學習效率。雖然它也處理標籤數據，但題目的關鍵點是「不會再標籤更多資料」，這與主動學習的概念相矛盾，因為主動學習的核心就是為了獲取更多有效標籤。

<img width="640" height="539" alt="image" src="https://github.com/user-attachments/assets/21aa0df4-9efc-449c-b74d-843a6c3e14ce" />

## 評估方法

Benchmark datasets (基準數據集): 評估模型性能，包括偏見和公平性的預定義、已標註的數據集。
Content moderation guidelines (內容審核指南): 內容審核指南是規則和原則
